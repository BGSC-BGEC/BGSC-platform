{
  "name": "bgsc-backend",
  "version": "1.0.0",
  "description": "BGSC platform backend \u2014 API gateway (:3000) plus domain services under apps/.",
  "main": "dist/index.js",
  "//preselfcheck": "ts-node does NOT apply tsconfig `paths` at runtime: Node resolves @bgsc/shared through the workspace symlink to packages/shared/dist, so every selfcheck and e2e runs against COMPILED shared code. Without these hooks, editing a shared model and running the suite tests the previous build \u2014 a changed invariant appears to pass before it exists.",
  "scripts": {
    "dev": "nodemon --watch src --watch packages/shared/src --ext ts --exec ts-node src/index.ts",
    "build": "npm run build --workspace @bgsc/shared && tsc --build",
    "start": "node dist/index.js",
    "typecheck": "npx -p typescript@7 tsc --build --force && npx -p typescript@7 tsc --noEmit -p tsconfig.check.json && npm run build",
    "preselfcheck": "npm run build --workspace @bgsc/shared",
    "selfcheck": "npm run selfcheck --workspaces --if-present && ts-node src/gateway/gateway.selfcheck.ts && node scripts/migrate-audit2.selfcheck.js",
    "pree2e": "npm run build --workspace @bgsc/shared",
    "e2e": "npm run e2e --workspaces --if-present",
    "test": "npm run selfcheck && npm run e2e",
    "smoke": "ts-node src/gateway/gateway.smoke.ts",
    "//seed:founder": "One-time bootstrap: promotes the registered, active account FOUNDER_EMAIL to founder. In the image: docker compose run --rm -e FOUNDER_EMAIL=... user-service node apps/user-service/dist/scripts/seed-founder.js",
    "seed:founder": "ts-node apps/user-service/src/scripts/seed-founder.ts",
    "//migrate:audit2": "Dry run by default; `npm run migrate:audit2 -- --apply` writes. Targets MONGO_URI. See scripts/migrate-audit2.js.",
    "migrate:audit2": "npm run build --workspace @bgsc/shared && node scripts/migrate-audit2.js"
  },
  "keywords": [],
  "author": "Rubber_Duck",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.1.0",
    "http-proxy-middleware": "^3.0.5",
    "ioredis": "^5.9.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.5",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^26.4.1",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
